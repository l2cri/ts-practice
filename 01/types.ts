const a1: string = '10'

type myIndex = number

interface SomeKing {
    a: boolean
    some?: boolean
}


type FunType = (foo: number) => boolean

type MyTypes = [number, undefined, null] 

let b2: string | myIndex

let optionObject0: { a: string, b?: number } = { a: 'foo' }

type OptionType = { need: boolean, can?: boolean }

